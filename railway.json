{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "node scripts/init-database.js && echo 'Database init complete' && echo 'Starting test server...' && node test-server.js"
  }
} 