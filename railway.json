{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "echo 'Starting test server first...' && node test-server.js & echo 'Test server started in background' && echo 'Now initializing database...' && node scripts/init-database.js && echo 'Database init complete' && echo 'Keeping container alive...' && wait"
  }
} 