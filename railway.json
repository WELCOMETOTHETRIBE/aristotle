{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "node scripts/init-database.js && echo 'Database init complete' && echo 'Testing basic server...' && node -e \"const http = require('http'); const server = http.createServer((req, res) => { res.writeHead(200, {'Content-Type': 'text/plain'}); res.end('Hello World!'); }); server.listen(3000, '0.0.0.0', () => console.log('Test server running on port 3000'));\""
  }
} 